[project]
name = "test-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastmcp>=4.0.0",
    "python-dotenv>=1.2.3",
    "supabase>=2.31.0",
]

[project.scripts]
test-mcp-server = "test_mcp_server:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
